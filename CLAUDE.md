# PPT as Code

> Clone 下来，用 Claude Code 打开，AI 自动引导你从零做出一套高质量 PPT。

这是一个 CLAUDE.md 驱动的 AI-人协作 PPT 工作台。AI 负责写 Prompt、生成图片、写记录；你负责选图、定方向、拍板。

---

## 每次对话开始

1. 读 `progress.md` → 存在则报告当前状态，继续工作
2. `progress.md` 不存在 → 进入 Phase 1 初始化

---

## Phase 1：项目初始化

按顺序完成四步：

### 1.1 API 配置

询问用户图片生成 API 信息，创建 `.env`：

```
API_URL=https://your-api-endpoint
API_KEY=your-api-key
MODEL=gemini-2.0-flash-preview-image-generation
IMAGE_SIZE=4K
```

确认 `.gitignore` 中包含 `.env`。

### 1.2 导入内容

引导用户将演讲稿或文案放入 `content/` 目录：
- 用户粘贴文本 → AI 创建 `content/script.md`
- 用户自己放文件 → AI 确认已读取

### 1.3 视觉风格

给用户提供预设方向，用户选择或自定义：

| 风格 | 特点 | 适合场景 |
|------|------|---------|
| **3D 渲染 / 电影概念艺术** | 深色背景、光影层次、金属质感文字 | 有冲击力的主题 |
| **扁平插画 / 现代设计** | 明快配色、几何图形、干净排版 | 商务、教育 |
| **摄影风格** | 真实照片背景、文字叠加 | 纪实、报告 |
| **极简 / 高端** | 大留白、精致字体、少量点缀 | 高端展示、提案 |

用户选定后，AI **直接编辑**下方「视觉风格」章节，写入：
- 核心意象（贯穿全篇的主题符号）
- 色调（主色、强调色、辅助色）
- 画面质感描述
- 文字排版规范（标题/正文的大小、颜色、质感）
- 重复元素定义（如导航栏：每个区块文字、高亮规则、样式）

### 1.4 页面规划

AI 读完 `content/` 全部内容后：
1. 建议分页方案（每页放什么内容，一句话概括）
2. 用户调整、确认
3. AI 在内容文档中插入页码标记（`<!-- 第01页：封面 -->` 格式），标注每页的内容边界
4. AI 创建目录结构：`ppt-pages/01/`、`ppt-pages/02/` ...
5. AI 创建 `progress.md`，阶段设为「首版制作」
6. 询问用户偏好（执行方式、Prompt 语言），写入下方「用户偏好」

完成后，**删除本章节（从 `## Phase 1` 到上方的 `---` 分隔线之间的全部内容）**，然后进入 Phase 2。

---

## Phase 2：首版制作（0 → 1）

**目标**：快速完成所有页面的第一版。先有完整的东西，再打磨。

**节奏**：80 分就过，不追求完美。

### 每页制作流程

1. **读计划** — 读 `progress.md`，确认当前做哪页
2. **读内容** — 读 `content/` 文档中对应页码标记的段落
3. **简短讨论** — 和用户确认画面构想（不过度纠结）
4. **参考图** — 询问用户是否使用参考图（建议使用），用哪一张
5. **写 gen.py** — 创建 `ppt-pages/XX/init/gen.py`
6. **生成图片** — 根据用户偏好自动执行或给出命令
7. **用户选图** — 生成完毕后，**必须问用户**：选哪张？满意吗？
8. **不满意** → 调整 prompt，在同目录新建 gen 脚本（如 `gen_v2.py`），重新生成
9. **满意** → AI 问"这一页结束了吗？"→ 用户确认结束 → AI 执行 `/done`

用户说"继续"时，读 `progress.md` 找到下一个未完成的页面。

所有页面完成后告知用户：首版制作完成，拿去展示或获取反馈，需要修改时说"开始修改"。

---

## Phase 3：迭代优化

**目标**：基于反馈，针对性修改特定页面。

**触发**：用户说"开始修改"、"我拿到反馈了"、"要改几页"等。

### 流程

1. **收集反馈** — 用户粘贴或描述收到的反馈
2. **写修改计划** — AI 和用户讨论后，创建 `ppt-pages/制作记录/修改计划-vN.md`（N 递增）
3. **逐页修改** — 不必按顺序，用户说改哪页就改哪页
   - 修改文件放入 `ppt-pages/XX/rev1/`（第二轮修改放 `rev2/`，以此类推）
   - 每页流程同 Phase 2（讨论 → 参考图 → gen.py → 生成 → 选图 → 确认结束 → `/done`）

可以有多轮修改，每轮独立的修改计划文档。

---

## 目录结构

### clone 下来的样子

```
ai-slides-engineering/
├── CLAUDE.md
├── .claude/commands/
│   └── done.md               # /done skill：完成当前页
├── tools/
│   └── gen_ppt_image.py      # 图片生成工具（纯 Python）
├── guide/                    # 方法论文档（选读）
│   ├── 01-philosophy.md
│   ├── 02-workflow.md
│   ├── 03-prompt-patterns.md
│   ├── 04-project-management.md
│   └── 05-iteration-strategy.md
├── README.md
├── LICENSE
└── .gitignore
```

### 使用后由 AI 创建

```
├── .env                           # API 配置（不入 Git）
├── content/                       # 演讲稿 / 文案
├── progress.md                    # 制作进度
├── ppt-ref-images/                # 参考图（风格锚定用）
└── ppt-pages/
    ├── 01/
    │   ├── init/                  # 首版制作
    │   │   ├── gen.py
    │   │   └── v1.png, v2.png, v3.png
    │   ├── rev1/                  # 第一次修改（如有）
    │   └── rev2/                  # 第二次修改（如有）
    ├── 02/
    │   └── init/
    └── 制作记录/
        ├── 修改计划-v2.md          # 第一轮修改计划（如有）
        ├── 首版/                   # Phase 2 每页制作记录
        │   ├── 01.md
        │   └── 02.md
        └── 修改/                   # Phase 3 每页修改记录
            ├── rev1-03.md
            └── rev1-08.md
```

---

## 视觉风格

<!-- Phase 1 完成后由 AI 替换以下内容 -->

**（待初始化 —— Phase 1 中由 AI 引导用户确定并填写此处）**

### 核心意象
### 色调
### 画面质感
### 文字排版
### 重复元素

---

## 用户偏好

<!-- Phase 1 首次交互时由 AI 询问并填写 -->

- 执行方式：（待确认 —— 自动 / 手动）
- Prompt 语言：（待确认 —— 默认中文）

---

## 图片生成

### gen.py 模板

每页的 `gen.py` 是独立可执行的 Python 脚本：

```python
#!/usr/bin/env python3
from pathlib import Path
import sys
sys.path.insert(0, str(Path(__file__).resolve().parents[3] / "tools"))
from gen_ppt_image import gen

PROMPT = """
（图片描述 Prompt）
"""

gen(prompt=PROMPT, page_dir=str(Path(__file__).parent), count=3)
```

### 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `prompt` | 图片描述 | 必填 |
| `page_dir` | 输出目录 | 必填 |
| `count` | 候选图数量 | 3 |
| `ref` | 参考图路径（用于风格锚定） | 无 |
| `image_size` | 分辨率 1K / 2K / 4K | 从 .env 读取 |
| `aspect_ratio` | 宽高比 | 16:9 |

### 参考图

- 已完成的页面可以导出到 `ppt-ref-images/`，作为后续页面的风格锚定
- gen.py 中通过 `ref="ppt-ref-images/xx.png"` 传入
- 参考图不是让 AI 照抄，而是锚定风格和布局
- 每页制作时 AI 应主动询问用户是否使用参考图（建议使用），并确认用哪一张

---

## progress.md 格式

```markdown
# 制作进度

## 当前阶段
首版制作 / 迭代优化（第N轮）

## 页面状态
| 页码 | 内容概要 | init | 选择 | rev1 | 选择 | rev2 | 选择 |
|------|---------|------|------|------|------|------|------|
| 01   | 封面    | done | v2   | done | v1   |      |      |
| 02   | 引言    | done | v3   |      |      |      |      |
| 03   | ...     | wip  |      |      |      |      |      |
```

轻量记录，只写页码、状态、用户选择。详细内容在制作记录里。

---

## Prompt 编写铁律

> 70+ 次实战迭代提炼，务必遵守。

1. **文字位置必须说清楚** — 不说"居中"，说"水平居中，垂直偏上三分之一处"
2. **颜色必须逐字指定** — 不说"金色标题"，说"「关键词A」金色，「关键词B」白色"
3. **主体元素优先级要强调** — 主体是什么必须明说（如"凤凰是画面主体，不能被文字遮挡"）
4. **参考图是最强的风格锚定** — 已确认的页面导出参考图，新页面上传参考图保持一致
5. **少改优于大改** — 已确认的页面用参考图 + 微调，不推翻重来
6. **重复元素每页都要完整描述** — 如导航栏的每个区块文字、高亮状态、样式，不能省略

---

## Prompt 经验（持续积累）

<!-- AI 在制作过程中发现有效的经验，追加到此处 -->

（随项目推进由 AI 自动添加）

---

## 重要规则

- 每页生成 3 张候选图，**必须让用户选择后才算完成**
- 每页完成后 AI 自动执行 `/done`，完成记录、进度更新、Git 提交
- 每页完成后**引导用户开新对话**，说"继续"做下一页
- 不提前批量创建 gen.py，逐页按需创建
- 每页制作时**主动询问是否使用参考图**（建议使用）
- AI 写的 Prompt 使用用户偏好的语言（默认中文）
