# Minimal Example

这个示例展示 `content/script.md` 可以长什么样。

使用方式：

1. 新建自己的 PPT 项目
2. 将 `examples/minimal/content/script.md` 复制到项目的 `content/script.md`
3. 让 agent 读取 `AGENTS.md` 或 `CLAUDE.md`
4. 按 Phase 1 初始化分页和视觉风格

这个示例只提供输入文案，不包含生成结果。
